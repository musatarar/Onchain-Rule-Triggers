"""On-chain constants shared across the EVM models."""

# The largest uint256 is 78 decimal digits.
UINT256_DIGITS = 78

# "0x" and the hex of a 32-byte hash, and of a 20-byte address.
HASH_LENGTH = 66
ADDRESS_LENGTH = 42
